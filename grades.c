#include <stdio.h>
/*prob 10370*/
int main(){
    int c, i, j, n, grade;
    double total, avg;
    scanf("%d",&c);
    for (i=0;i<c;i++){
        scanf("%d", &n);
        for (j=0;j<n;j++){
            scanf("%d", &grade);
            total+=grade;
        }
        avg = total/n;
        while
    }
}